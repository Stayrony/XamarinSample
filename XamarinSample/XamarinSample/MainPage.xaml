<?xml version="1.0" encoding="utf-8" ?>
<MasterDetailPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinSample.MainPage"
             xmlns:local="clr-namespace:XamarinSample.Views;assembly=XamarinSample"
             Title="Xamarin Sample">
  <MasterDetailPage.Master>
    <ContentPage Title="Menu" x:Name="masterPage">
      <ContentPage.Content>
        <StackLayout VerticalOptions="FillAndExpand">
          <ListView x:Name="listView" VerticalOptions="FillAndExpand" SeparatorVisibility="None">
            <ListView.ItemTemplate>
              <DataTemplate>
                <ImageCell Text="{Binding Title}"  TextColor="Gray" ImageSource="{Binding IconSource}" />
              </DataTemplate>
            </ListView.ItemTemplate>
          </ListView>
        </StackLayout>
      </ContentPage.Content>
    </ContentPage>
  </MasterDetailPage.Master>

  <MasterDetailPage.Detail>
    <local:HomePage/>
  </MasterDetailPage.Detail>
</MasterDetailPage>